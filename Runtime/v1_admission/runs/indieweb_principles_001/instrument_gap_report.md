# Instrument Gap Report

```text
status: instrument gap report / foreign input feedback
gap: v0.3 lacks NormativeClaim chart basis
instrument: v1_admission_wrapper
```

## A. Finding

```text
The first foreign input expresses many principles as imperative/directive norms rather than DME-style distinction assertions.

Existing v0.3 classes DEF/NC/BL/OP/DEP do not adequately type imperative principles such as "Own your data," "Use what you make," "Build for the long web," or "Document your stuff."

The instrument was built around a corpus that asserts distinctions. It met a corpus that asserts obligations.
```

## B. Candidate Receptor, Not Implemented

```text
NormativeClaim candidate shape:
actor SHOULD action WITHIN scope BECAUSE rationale
```

## C. Non-Collapse

```text
normative principle != DME doctrine
imperative text != admitted law
foreign value != DME canon
untyped norm != extraction failure
instrument gap != theory failure
thin wrapper finding != full v0.3 extractor capability
```

## D. v0.4 Pressure

```text
v0.4 pressure observed:
Add a candidate NormativeClaim chart basis only after a second foreign input confirms the gap generalizes.
```

## E. Reflexive Finding

```text
A non-collapse law is a NormativeClaim with elided rationale:
"A != B" unfolds to: "do not treat A as B, because they differ."

The receptor foreign input demands is one DME's own corpus would also populate.
NormativeClaim is not merely an import adapter.
It is a latent native class surfaced by foreign contact.
This strengthens the v0.4 case but does NOT authorize building it from one source.
```
