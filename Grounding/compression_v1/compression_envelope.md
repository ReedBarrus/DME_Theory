# Compression Envelope

```text
CompressionEnvelope: doctrine NC authority compression
source_snapshot: theory-v2.1-grounded-canon (input);
  new anchor after operation
operation: operating point A - authority compression

invariants (hard, +inf distortion on violation):
  content: every unique NC law preserved verbatim
  section-role: every document's non-collapse sections
    untouched and fully legible in place
  provenance: every instance's location preserved in
    inventory and registry instance maps
  canonical-chain: exactly one canonical entry per content

declared loss (the compression):
  peer-authority status of duplicate occurrences.
  Before: N peer assertions. After: 1 canonical entry +
  N declared projections. Loss is on the authority axis
  only; all epistemic axes at zero distortion.

side channel exercised: registry instance maps +
  claim inventory (the Ledger, per FCL-003 central finding)

authority_basis: operation authorized by repository
  authority ceiling (maintainer-issued); canonicality
  declaration is an authority act under Support and
  Authorization - recorded as such, distinct from any
  epistemic status.
```
